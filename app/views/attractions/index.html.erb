
<% Attraction.all.each do |a| %>
<%= link_to "Go on #{a.name}", a %><br/><br/>
  <% if current_user.admin %>
    <%= link_to "Show #{a.name}", a %><br/><br/>
  <% else %>
    <%= link_to "Go on #{a.name}", a %><br/><br/>
  <% end %>
<% end %>

<% if current_user.admin %>
  <%= link_to "New Attraction", new_attraction_path%>
<% end %>
